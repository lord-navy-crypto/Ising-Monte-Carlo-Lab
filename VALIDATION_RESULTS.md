# Validation Results — through 1.1.2

Validation executed in the build environment after the optimization pass.

## Automated checks

- Core pytest suite: **22 passed**.
- Python compilation: `app.py`, `ising_lab/core.py`, `ising_lab/__init__.py`, and tests parsed/compiled successfully.
- macOS launcher shell syntax: passed `bash -n`.
- macOS launcher executable mode: preserved as `755`.
- Full Streamlit AppTest suite was not collected in this isolated build environment because `streamlit` is not installed here. GitHub Actions installs `requirements-dev.txt` before running the complete suite.

## Numerical spot checks

### Odd 1D periodic ring, N=15, T=1.5

Finite-N transfer-matrix reference:

`E/N = -0.5831270948678592`

After the heat-bath odd-lattice repair, a long run gave energy within a few `1e-3` of the exact result.

After replacing the biased Wolff stopping-time sweep with one fixed cluster transition per update cycle, long-run Wolff energies for several seeds were also within roughly `1e-3`–`1e-2` of the exact result.

Checkerboard Metropolis now rejects this odd periodic lattice with a clear compatibility error instead of producing an invalid parallel update.

### 1D finite-size reference semantics

For `T=0.9`, exact transfer-matrix energies for `N = 4, 5, 8` are distinct and the finite-size scan now preserves those size-specific values instead of copying the largest-N reference.

### Multi-chain convergence smoke check

A 2D heat-bath run at `N=8`, `T=2.8` using random, all-plus, and all-minus starts produced finite R-hat values near one for both energy and `|M|/N`, confirming the new diagnostic path executes and compares chains consistently.


## 1.1.1 display-only regression

The 1.1.1 patch does not change the Monte Carlo or exact-physics engine. It changes result presentation.

Checks performed after the patch:

- all Python files parse successfully;
- `python -m py_compile app.py ising_lab/core.py ising_lab/__init__.py` passes;
- the 22 core physics/statistics tests still pass;
- result-output `st.metric` cards were removed in favor of vertical data tables;
- the major result plots are no longer placed side-by-side;
- 1D/2D temperature scans and finite-size scans now expose complete long-form values;
- method comparison, equilibration, snapshot, external-data, and validation pages expose complete numerical tables;
- displayed floating-point values use round-trip `repr(float)` strings rather than shortened `:.4g` / `:.6g` formatting;
- snapshot and diagnostic final spin configurations are listed by coordinate in addition to graphical heatmaps.

Full Streamlit `AppTest` execution still requires the Streamlit dependency installed by `requirements-dev.txt` / GitHub Actions.

## 1.1.2 trend-first progressive-display regression

The 1.1.2 patch changes presentation flow only; the Monte Carlo and exact-physics engine is unchanged from 1.1.1.

Checks performed after the patch:

- `python -m py_compile app.py ising_lab/core.py ising_lab/__init__.py` passes;
- AST parsing of every Python file passes;
- macOS launcher passes `bash -n`;
- core physics/statistics suite remains **22/22 passed**;
- new display-flow regression tests add **2 passed** checks, for **24/24 non-Streamlit tests passed** total;
- every major visual-result page is statically checked to place its plot/heatmap before the default numerical summary;
- all complete-data views are gated by explicit `More data / complete ...` buttons rather than being generated by default;
- key tables keep full numeric string representations while limiting the number of visible quantities/rows;
- complete long-form tables, raw arrays, trajectories, spin coordinates, and CSV exports remain available after the user requests them.

Full Streamlit `AppTest` execution is still not run in this isolated build environment because `streamlit` is not installed here. The repository test configuration still includes the Streamlit app tests for environments that install `requirements-dev.txt`.

## v1.1.3 GitHub-ready repository checks

- Repository-root layout prepared for direct Git initialization: PASS
- GitHub Actions workflow YAML parse: checked structurally
- Python source compilation (`app.py`, `ising_lab`, `tools`, `tests`): PASS
- Automatic free-port helper unit test: PASS in local validation
- macOS launcher executable bit preserved: PASS
- macOS launcher shell syntax: PASS
- Complete non-Streamlit numerical/display/launcher test subset: PASS (see package-generation validation log)
- Full Streamlit `AppTest` requires the Streamlit dependency; GitHub CI installs `requirements-dev.txt` before running it.
